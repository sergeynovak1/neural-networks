# Типы интеллектуальных агентов (по Норвигу и Расселу)

> **Определения**:
> 
> **Агент** — это сущность, которая воспринимает окружающую среду через сенсоры (входные данные) и воздействует на нее через исполнительные механизмы (выходные действия).
> 
> **Перцепт** (percept) — это входное восприятие агента в текущий момент времени, получаемое через сенсоры. Последовательность перцептов образует историю восприятий.
> 
> **Внутреннее состояние** — это внутреннее представление агента о текущем состоянии мира, которое может включать информацию о ненаблюдаемых в данный момент аспектах среды. Обновляется на основе новых перцептов и предыдущего состояния.
> 
> **Модель мира** — это внутреннее представление агента о внешней среде, включая ее законы, структуру, возможные состояния и последствия своих действий. Это упрощенная, но полезная копия реальности, необходимая для принятия решений. Модель мира используется для обновления внутреннего состояния и предсказания последствий действий.
> 
> **Правило condition-action** (правило "условие-действие") — это правило вида "если выполняется условие C, то выполнить действие A". Простые рефлекторные агенты используют прямую таблицу таких правил без учета истории.
> 
> **Таблица переходов** — это таблица соответствий между перцептами (или состояниями) и действиями. В простейшем случае это таблица "восприятие → действие".
> 
> **Функция полезности** (utility function) — это функция U(s), которая присваивает каждому состоянию s численную оценку, отражающую предпочтительность этого состояния для агента. Более высокое значение функции полезности соответствует более предпочтительному состоянию.
> 
> **Ожидаемая полезность** (expected utility) — это математическое ожидание функции полезности для возможных исходов действия, учитывающее вероятности различных исходов. Агент стремится максимизировать ожидаемую полезность при выборе действия.

---

## 📚 Оглавление

1. [Классификация агентов](#классификация-агентов)
2. [Типы интеллектуальных агентов](#типы-интеллектуальных-агентов)
   - [Простые рефлекторные агенты 🤖](#простые-рефлекторные-агенты-🤖)
   - [Модельные рефлекторные агенты 🧠](#модельные-рефлекторные-агенты-🧠)
   - [Агенты на основе цели 🎯](#агенты-на-основе-цели-🎯)
   - [Агенты на основе полезности 📊](#агенты-на-основе-полезности-📊)
   - [Обучающиеся агенты 🎓](#обучающиеся-агенты-🎓)
3. [Сравнение агентов](#сравнение-агентов)
   - [Технические характеристики](#технические-характеристики)
   - [Функциональные характеристики](#функциональные-характеристики)
4. [Практические рекомендации по выбору типа агента](#практические-рекомендации-по-выбору-типа-агента)
5. [Заключение](#заключение)

## Классификация агентов

Классификация основана на степени автономности, внутренней сложности и способности агента справляться с различными типами сред. Критерии классификации включают: наличие внутреннего состояния, способность учитывать историю, наличие целей или функции полезности, и способность к обучению. Эта классификация представляет иерархию от простейших реактивных систем до полностью автономных обучающихся агентов.

<div style="border: 2px solid; padding: 15px; margin-bottom: 20px;">

## Простые рефлекторные агенты 🤖 (Simple Reflex Agents)

**Как работают:** Действуют по принципу «стимул-реакция». Прямое сопоставление текущего перцепта с правилом condition-action без учета истории и внутреннего состояния. Основываются на таблице переходов "перцепт → действие". Математически можно описать как функцию action = f(percept), где f определяется таблицей правил.

**Формальное описание:** Агент выбирает действие a на основе текущего перцепта p согласно правилу: если percept соответствует условию C, то выполнить действие A. Не существует функции состояния update_state() — агент полностью определяется текущим входом.

**Плюсы:** Простота реализации, высокая скорость реакции, низкие вычислительные требования.

**Минусы:** Крайне ограничены. Работают только в полностью наблюдаемой среде (fully observable environment), где текущий перцепт содержит всю необходимую информацию для принятия правильного решения. Невозможны в частично наблюдаемых средах.

**Примеры:** 
- Дверь-автомат (датчик видит движение → мотор открывает дверь) — перцепт содержит информацию о наличии движения, этого достаточно для действия.
- Термостат, который включает отопление при температуре ниже 20°C (датчик температуры → включение нагревателя) — текущая температура полностью определяет необходимое действие.

**Когда использовать:** Простые автоматизированные системы в полностью контролируемых и наблюдаемых средах, где реакция на текущее состояние достаточна для достижения цели.

</div>

<div style="border: 2px solid; padding: 15px; margin-bottom: 20px;">

## Модельные рефлекторные агенты 🧠 (Model-Based Reflex Agents)

**Как работают:** У них есть внутренняя модель мира и внутреннее состояние. Агент поддерживает состояние state, которое обновляется на основе перцепта и предыдущего состояния: state = update_state(state, percept). Модель мира используется для предсказания, как мир изменяется в результате действий агента и независимо от него. Действие выбирается на основе текущего состояния: action = f(state).

**Связь между моделью мира и внутренним состоянием:** Модель мира — это знания агента о том, как устроен мир (законы физики, правила игры, структура среды). Внутреннее состояние — это конкретное представление текущей ситуации на основе модели мира и истории перцептов. Модель мира используется для корректного обновления состояния при получении новых перцептов.

**Формальное описание:** Агент определяет два компонента: функцию обновления состояния update_state(state, percept) и функцию выбора действия action = choose_action(state). Модель мира неявно закодирована в функции update_state, которая использует знание о том, как мир эволюционирует.

**Отличие от простых:** Способен действовать в частично наблюдаемых средах (partially observable environment), так как внутреннее состояние хранит информацию о ненаблюдаемых в данный момент аспектах.

**Примеры:** 
- Робот-пылесос, который помнит, какие комнаты он уже убрал (состояние включает карту убранных комнат), даже если сейчас их не видит, и строит оптимальный маршрут уборки на основе этой информации.
- Автомобиль с адаптивным круиз-контролем, который отслеживает положение других автомобилей (состояние включает позиции других машин) даже когда они временно скрыты за поворотом, используя модель движения транспорта.

**Когда использовать:** Системы, работающие в частично наблюдаемых средах, где необходимо учитывать историю для принятия правильных решений.

</div>

<div style="border: 2px solid; padding: 15px; margin-bottom: 20px;">

## Агенты на основе цели 🎯 (Goal-Based Agents)

**Как работают:** У них есть модель мира, внутреннее состояние и цель (goal) — желаемое состояние или множество желаемых состояний. Агент оценивает действия не только с точки зрения текущего состояния, но и их способности привести к цели. Используют алгоритмы поиска (поиск в пространстве состояний, планирование) для определения последовательности действий, ведущих к достижению цели. Это вводит понятие планирования (planning).

**Формальное описание:** Агент имеет функцию test_goal(state), которая возвращает true, если состояние соответствует цели. Для выбора действия агент ищет последовательность действий [a₁, a₂, ..., aₙ] такую, что выполнение этой последовательности из текущего состояния приводит к состоянию, где test_goal(state) = true.

**Отличие от модельных рефлекторных:** Решает вопрос «что делать» (какую последовательность действий выбрать для достижения цели), а не просто «как отреагировать» на текущее состояние. Вводит понятие планирования и поиска в пространстве состояний.

**Примеры:** 
- Навигатор (GPS). У него есть модель карты (граф дорог), текущее местоположение (состояние) и цель «достичь точки Б». Он планирует последовательность действий (поворотов), используя алгоритмы поиска кратчайшего пути (например, A* или Dijkstra), чтобы достичь цели.
- Робот-доставщик, который должен добраться до определенного офиса в здании. Цель — "быть в офисе X", агент планирует маршрут через коридоры и лифты, оценивая различные пути достижения цели.

**Когда использовать:** Задачи, требующие планирования последовательности действий для достижения конкретной цели, особенно когда существует множество возможных путей к цели.

</div>

<div style="border: 2px solid; padding: 15px; margin-bottom: 20px;">

## Агенты на основе полезности 📊 (Utility-Based Agents)

**Как работают:** У агента есть функция полезности U(s), которая присваивает каждому состоянию s численную оценку. Агент стремится максимизировать ожидаемую полезность E[U(s') | a], где s' — возможные состояния после выполнения действия a. Функция полезности позволяет сравнивать различные состояния и пути, выбирая оптимальный с точки зрения полезности.

**Связь с агентами на основе цели:** Агенты на основе цели решают задачу достижения конкретной цели (бинарная функция: цель достигнута или нет). Агенты на основе полезности более гибки: они могут выбирать между разными способами достижения цели или даже между разными целями, если есть несколько конкурирующих целей. Функция полезности позволяет количественно оценить предпочтительность различных исходов.

**Формальное описание:** Агент выбирает действие a* = argmaxₐ E[U(s') | a], где ожидание берется по возможным исходам действия a. В детерминированной среде это упрощается до: a* = argmaxₐ U(result(a, state)), где result(a, state) — результат выполнения действия a в состоянии state.

**Отличие от агентов на основе цели:** Решает вопрос «какую из целей достигать лучше всего» или «как лучше достичь цели», когда существует несколько конкурирующих целей или несколько путей с разной предпочтительностью. Позволяет учитывать не только достижение цели, но и качество пути к ней (например, стоимость, время, риск).

**Примеры:** 
- Навигатор, который выбирает не просто любой путь из А в Б, а оптимальный с точки зрения полезности — самый короткий, самый быстрый или без пробок. Функция полезности может учитывать время в пути, расход топлива, платные участки и т.д., максимизируя общую полезность маршрута.
- Агент для торговли акциями: цель «получить прибыль» слишком абстрактна, а функция полезности U(прибыль, риск) помогает выбрать между рискованной сделкой с высоким доходом и стабильной, балансируя доходность и риск.
- Рекомендательная система Netflix, которая балансирует между знакомым контентом и новыми интересными фильмами для максимизации ожидаемой полезности (например, времени просмотра или удовлетворенности пользователя).

**Когда использовать:** Задачи с несколькими конкурирующими целями или когда качество пути к цели важно, особенно в стохастических средах, где необходимо учитывать вероятности различных исходов.

</div>

<div style="border: 2px solid; padding: 15px; margin-bottom: 20px;">

## Обучающиеся агенты 🎓 (Learning Agents)

**Как работают:** Это не отдельный тип агента, а архитектурная надстройка над любым из предыдущих типов (рефлекторным, целевым, полезностным). Обучающийся агент может улучшать свои характеристики на основе опыта взаимодействия со средой. Состоит из четырех компонентов, которые работают в цикле обучения:

1. **Элемент обучения** (Learning Element): Анализирует опыт (последовательность состояний, действий и результатов) и извлекает из него знания. Сравнивает результаты действий с ожидаемыми и определяет, что нужно улучшить. Отвечает за обновление базы знаний на основе опыта.

2. **Исполнительный элемент** (Performance Element): Собственно агент (рефлекторный, целевой, полезностный и т.д.), который действует в среде. Принимает перцепты, выбирает и выполняет действия. Это может быть любой из типов агентов, описанных выше.

3. **Генератор проблем** (Problem Generator): Предлагает новые, возможно нестандартные действия для исследования среды и сбора данных. Играет роль баланса между эксплуатацией (exploitation) известных хороших стратегий и исследованием (exploration) новых возможностей. Это важно для обучения в новых или изменяющихся средах.

4. **База знаний** (Knowledge Base): Хранилище знаний агента (правила, параметры модели, веса нейронной сети и т.д.), которое обновляется элементом обучения. Может включать модель мира, функцию полезности, правила condition-action, параметры алгоритма планирования и т.д.

**Взаимодействие компонентов:** Исполнительный элемент действует в среде и получает обратную связь (награды, результаты). Элемент обучения анализирует эту информацию и обновляет базу знаний. Генератор проблем предлагает исследовательские действия, чтобы улучшить качество обучения. Обновленная база знаний используется исполнительным элементом для улучшения будущих действий.

**Современные подходы:** Современные обучающиеся агенты часто используют методы машинного обучения, включая глубокое обучение (deep learning) и обучение с подкреплением (reinforcement learning). В reinforcement learning функция полезности (награда) задается извне, а агент учится оптимальной политике (policy) — правилу выбора действий.

**Примеры:** 
- Самообучающийся игровой ИИ (например, AlphaZero). Исполнительный элемент — агент, играющий в игру. Элемент обучения анализирует игры и обновляет параметры нейронной сети (база знаний), которая оценивает позиции и предлагает ходы. Генератор проблем обеспечивает исследование различных стратегий, играя сам с собой. Агент начинал с нулевых знаний и достиг суперчеловеческого уровня через самообучение.

- Рекомендательная система, которая учится предпочтениям пользователя: исполнительный элемент рекомендует контент, элемент обучения анализирует реакции пользователя (клики, просмотры, рейтинги) и обновляет модель предпочтений, генератор проблем иногда рекомендует новый контент для исследования вкусов пользователя.

**Когда использовать:** Задачи, где правила или оптимальная стратегия неизвестны заранее или изменяются со временем, и где агент может собирать опыт взаимодействия со средой для улучшения своих характеристик.

</div>

## Технические характеристики

| Тип агента | Внутреннее состояние | Цель/Полезность | Обучение | Сложность реализации | Пример среды |
|------------|----------------------|------------------|----------|-------------------|--------------|
| Простой рефлекс | Нет | Нет | Нет | Низкая | Лабиринт с видимыми стенами |
| Модельный рефлекс | Есть (модель мира) | Нет | Нет | Средняя | Лабиринт с частичной видимостью |
| На основе цели | Есть | Есть цель | Нет | Выше среднего | Лабиринт, нужен выход в точке Б |
| На основе полезности | Есть | Есть функция полезности | Нет | Высокая | Лабиринт с монетами (надо выйти, собрав максимум) |
| Обучающийся | Есть | Любой из выше | Есть | Очень высокая | Неизвестная среда, правила которой надо понять |


## Функциональные характеристики

| Тип агента | Память | Цель | Полезность | Обучение |
|------------|--------|------|------------|----------|
| Простой рефлекторный | Нет | Нет | Нет | Нет |
| С состоянием | Есть | Нет | Нет | Нет |
| На основе цели | Есть | Есть | Нет | Нет |
| На основе полезности | Есть | Есть | Есть | Нет |
| Обучающийся | Есть | Есть | Есть | Есть |


## Практические рекомендации по выбору типа агента

Выбор типа агента зависит от характеристик задачи и среды:

- **Простой рефлекторный агент**: Используйте для простых автоматизированных систем в полностью наблюдаемых и контролируемых средах, где реакция на текущее состояние достаточна.

- **Модельный рефлекторный агент**: Подходит для частично наблюдаемых сред, где необходимо учитывать историю, но достаточно реактивного поведения без явного планирования.

- **Агент на основе цели**: Выбирайте, когда требуется планирование последовательности действий для достижения конкретной цели, особенно если существует множество возможных путей.

- **Агент на основе полезности**: Используйте, когда нужно выбирать между конкурирующими целями или когда качество пути к цели важно. Особенно полезен в стохастических средах с неопределенностью исходов.

- **Обучающийся агент**: Выбирайте для задач, где оптимальная стратегия неизвестна или изменяется, и где есть возможность собирать опыт для улучшения. Может комбинироваться с любым из предыдущих типов как базовым исполнительным элементом.

## Заключение

Классификация агентов по Норвигу и Расселу представляет иерархию от простейших реактивных систем до полностью автономных обучающихся агентов. Каждый тип агента представляет собой компромисс между простотой реализации, вычислительными требованиями и способностью решать сложные задачи в различных типах сред.

**Ключевые выводы:**
1. Сложность агента определяется наличием внутреннего состояния, способностью учитывать историю, наличием целей или функции полезности, и способностью к обучению.

2. Выбор типа агента зависит от характеристик среды (полностью/частично наблюдаемая, детерминированная/стохастическая, статическая/динамическая) и требований задачи.

3. На практике большинство современных интеллектуальных систем сочетают в себе черты нескольких типов агентов. Особенно активно используются обучающиеся агенты с элементами полезности и целей, что позволяет системам адаптироваться к изменяющимся условиям и оптимизировать свое поведение.

4. Обучающиеся агенты представляют наиболее перспективное направление, так как могут улучшать свои характеристики на основе опыта, что критически важно для работы в сложных и изменяющихся средах.

Эта классификация служит основой для понимания архитектур интеллектуальных систем и принятия решений о выборе подходящего типа агента для конкретной задачи.
